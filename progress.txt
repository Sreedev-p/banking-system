inside user ->
    adduser methode :
        need to modify when age<18
    deposit method
        change so that bank modify balance in a file an user read from it
need the logic to assign account no

db.txt format:
    AccountNumber,Name,Age,Balance,Password,Pin

